import { getAxios } from "../api/restApi"
import { Status } from "../enum/enum"

export const About = ()=>{


    return <div> 여기는 About 입니다.
        <button className="" onClick={()=>{}}>로그아웃버튼</button>
    </div>
}